import React, { useEffect, useState } from "react";

import "./PR.css";

const PR = ({}): React.JSX.Element => {
    const [metric, setMetric] = useState(0);

    useEffect(() => {
        
    }, [metric])
    
    return (
        <>

        </>
    );
}

export default PR;